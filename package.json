{
  "name": "bug-hunter-rsi",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "deploy": "wrangler deploy",
    "executor": "node executor/hunt-fix.mjs"
  }
}
